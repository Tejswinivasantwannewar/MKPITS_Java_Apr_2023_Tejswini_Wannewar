#include<stdio.h>
#include<conio.h>
void main()
{

float pi=3.14,c=2,circum,rad;   //if we are taking integer value in fraction then we have to take radius as float i taking as digit the it will be in int//

printf("enter num to calculate circumference of circle:");//formula=2*pi*r//
scanf("%f",&rad);
  circum=c*pi*rad;
printf("the vol of sphere is = %f",circum);
getch();
}
